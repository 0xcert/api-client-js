# Contributing

## Release process

1. Update `@0xcert` packages in `package.json`.

2. Change package version in `package.json`.

3. Build and publish.

```sh
$ npm i
$ npm run build
$ npm publish
```
